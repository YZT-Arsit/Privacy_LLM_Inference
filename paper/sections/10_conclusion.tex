% ===========================================================================
% CONCLUSION --- WRITE LAST (after abstract). May contain ONLY claims already in
% claim_ledger.md, with wording compatible with the body. This file holds a plan.
% ===========================================================================
\section{Conclusion}
\label{sec:conclusion}

% PLAN (fill last):
%  - Restate the construction (masked accelerator path + trusted boundary) and
%    the CALIBRATED result set: algebraic correctness (float-verified), the
%    leakage characterization (what stays visible), the LoRA/optimizer boundary,
%    and the measured-vs-projected cost separation.
%  - Restate the honest security posture: masks are not a token-privacy
%    mechanism under public weights; the trusted layer-0 relocation is.
%  - Non-claims block (mirror abstract + Sec~\ref{sec:security}).
%  - Future work: real CC-TEE deployment + real wall-time; full production
%    fine-tune; heterogeneous per-module padded rank; stronger learned
%    inverters/rank detectors; mr_td policy pinning; sampling-mode correctness.
\TODO{Write Conclusion last, after abstract; pull only ledger-backed claims.}
