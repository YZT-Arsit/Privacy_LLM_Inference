% ===========================================================================
% CORRECTNESS ANALYSIS --- argument skeleton (skill S8 correctness half + S6
% math protocol). Show the transformed execution recovers the intended
% plaintext computation. Distinguish EXACT symbolic equality from FLOATING-POINT
% closeness (state magnitude + dtype for every identity). Proofs go to
% appendices/proofs.tex; state theorems + failure conditions here.
% ===========================================================================
\section{Correctness Analysis}
\label{sec:correctness}

% CENTRAL QUESTION: Under which stated conditions does masked execution reproduce
% the plaintext model output, and at what numerical precision?

% ---- 6.0 Conventions -------------------------------------------------------
% Row-vector convention; per-layer object types (invertible vs orthogonal vs
% permutation vs paired-permutation vs additive pad). \evid{C-COR-CONV}
% \risk{COR-01: LoRA backward proof terse -- state convention, expand U^{-1}/N_out^{-1} cancellation.}

% ---- 6.1 Linear + boundary pad (Thm 1) -------------------------------------
% \evid{C-COR-01} Proven; fp64 max abs err 1.07e-14; fp32 6.68e-6; real Qwen-0.5B
% (CPU,fp32) recovered-logit max abs err 1.79e-7, greedy token match 1.0.

% ---- 6.2 Nonlinear islands (Thm 2 pointwise perm; Thm 3 SwiGLU paired perm) -
% \evid{C-COR-06,07,08} Pointwise permutation island EXACT (0.0). SwiGLU paired
% perm fp64 1.78e-15. NEG control (mismatched perm) MUST fail (state it).
% \risk{COR-02: include dense-mask non-commutation counterexample.}

% ---- 6.3 Norm islands (Thm 4 RMSNorm orthogonal; Thm 5 LayerNorm mean-pres.) -
% \evid{C-COR-NORM} RMSNorm commutes with orthogonal N only if gain folded;
% VECTOR gamma does NOT commute (measured error 9.68). Impossibility: exact
% input-independent RMSNorm-covariant obfuscation forces N N^T = cI => per-token
% norm leakage unavoidable (analytically_estimated, scoped). \risk{Wired path
% uses TRUSTED recompute (trusted_fallback_with_repad), not a GPU island.}

% ---- 6.4 Attention / RoPE / GQA / KV-cache (Thm 6) -------------------------
% \evid{C-COR-02,03,04,05} QK invariance under N_Q N_K^T=I (fp64 ~1e-14/1e-16);
% RoPE commutes only with 2x2-block masks; GQA mask-tying; KV-append distributes
% over token axis. KV mask FIXED within session (restriction).
% \risk{CF-COR-KV: design.md "fresh per token" vs KV-fixed restriction.}

% ---- 6.5 LoRA forward / backward (Thm 7, Thm 8) ---------------------------
% \evid{C-LORA-FWD,C-LORA-BWD} Forward fp64 ~1e-14; gradient recovery fp64
% ~1e-16..1e-15 (SYNTHETIC linear only). Nonlinear masked backward NOT implemented.

% ---- 6.6 Rank padding (Thm 9) ---------------------------------------------
% \evid{C-LORA-RANK} A_pad B_pad = A B, dummy contribution norm ~0 (fp64).
% \risk{Dummy-strategy count inconsistent ("four"/six/seven) -- fix to actual 7.}

% ---- 6.7 Autoregressive generation ----------------------------------------
% \evid{C-COR-GEN} GREEDY only: sequence exact match 1.0. \risk{Do NOT
% generalize to sampling/top-k/top-p/beam -- unsupported by artifacts.}

% ---- 6.8 What correctness does NOT claim ----------------------------------
% Not indistinguishability; not all inputs; not untested architectures; not
% bit-exactness (float-close); not real GPU/bf16 kernels (all CPU/fp64|fp32).
\TODO{Author after notation unified and float-vs-exact table (tables/) generated.}
