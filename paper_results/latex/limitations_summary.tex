% Auto-generated by paper_artifact_consolidation
\begin{table}[h]
\centering
\small
\caption{Limitations Summary}
\label{tab:limitations\_summary}
\begin{tabular}{llllll}
\toprule
category & limitation & affected\_claim & severity & paper\_wording & artifact\_support \\
\midrule
workload\_profile & Simulated TEE cost model — not real SGX wall-clock. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/workload\_profile.json \\
workload\_profile & Wall time for tslp\_baseline, ours\_ideal, and amulet\_style\_reference is projec... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/workload\_profile.json \\
workload\_profile & tiny-gpt2 (n\_layer=2, n\_embd=2, n\_head=2) is far smaller than production GPT-2. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/workload\_profile.json \\
workload\_profile & FLOP / byte proxies use coarse constants; absolute numbers are illustrative. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/workload\_profile.json \\
workload\_profile & Qwen / Llama not yet covered — see Stage 5.4 roadmap. & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/workload\_profile.json \\
workload\_profile & amulet\_style\_reference is a reference cost model, not a re-implementation of ... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/workload\_profile.json \\
nonlinear\_island\_security & These experiments are security proxies, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & Compatible mask families are weaker than unrestricted dense masks inside nonl... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & Permutation islands hide channel identity but do not hide coordinate-value mu... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & Orthogonal masks preserve row norms by design. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & Mean-preserving orthogonal masks preserve row means and centered norms by des... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & Fresh permutation reduces stable statistical recovery but does not provide de... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & Dense sandwiching and pad at Linear boundaries are required mitigations. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & This stage does not implement adaptive learned inversion attacks. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & This stage does not implement real TEE isolation. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
nonlinear\_island\_security & This stage does not prove semantic security. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/nonlinear\_island\_security.json \\
adaptive\_island\_attacks & These are adaptive/proxy attacks, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/adaptive\_island\_attacks.json \\
adaptive\_island\_attacks & The attacks use synthetic structured channel data, not full real-model activa... & other & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/adaptive\_island\_attacks.json \\
adaptive\_island\_attacks & No adaptive black-box querying of a deployed LLM is implemented. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/adaptive\_island\_attacks.json \\
adaptive\_island\_attacks & No side-channel attack is implemented. & hardware\_sidechannel\_security & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/adaptive\_island\_attacks.json \\
adaptive\_island\_attacks & No real TEE isolation is evaluated. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/adaptive\_island\_attacks.json \\
adaptive\_island\_attacks & Dense sandwiching reduces tested recovery but does not imply semantic security. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/adaptive\_island\_attacks.json \\
adaptive\_island\_attacks & Default-on recommendations are conditional on the tested threat model only. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/adaptive\_island\_attacks.json \\
modern\_decoder\_probe & Compatible mask families are weaker than unrestricted dense masks. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/modern\_decoder\_probe.json \\
modern\_decoder\_probe & Fresh permutation, dense sandwiching, and pad at Linear boundaries are requir... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/modern\_decoder\_probe.json \\
modern\_decoder\_probe & Probe-level migration only — not a full Qwen / TinyLlama wrapper integration. & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/modern\_decoder\_probe.json \\
modern\_decoder\_probe & No Qwen / TinyLlama generation path is implemented. & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/modern\_decoder\_probe.json \\
modern\_decoder\_probe & GQA / MQA is tensor-level only, not full runtime KV cache integration. & other & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/modern\_decoder\_probe.json \\
modern\_decoder\_probe & Compatible islands inherit Stage 5.4 mitigation requirements: fresh permutati... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/modern\_decoder\_probe.json \\
modern\_decoder\_probe & This is not a real TEE measurement. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/modern\_decoder\_probe.json \\
modern\_decoder\_probe & This is not formal security. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/modern\_decoder\_probe.json \\
real\_activation\_attacks & These are real-activation adaptive proxy attacks, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_activation\_attacks & If synthetic fallback is used, results are not real Qwen/TinyLlama activation... & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_activation\_attacks & Random hidden-state block input is not the same as real token distribution un... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_activation\_attacks & No black-box query attack is implemented. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_activation\_attacks & No side-channel attack is implemented. & hardware\_sidechannel\_security & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_activation\_attacks & No real TEE isolation is evaluated. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_activation\_attacks & Dense sandwiching reduces tested recovery but does not imply semantic security. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_activation\_attacks & This stage does not implement full model-level generation or KV cache runtime. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_activation\_attacks & fresh\_perm\_only and fresh\_perm\_plus\_sandwich\_plus\_pad share the same per-call... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_activation\_attacks.json \\
real\_token\_activation\_attacks & These are real-token-prompted adaptive proxy attacks, not formal security pro... & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & If synthetic token fallback is used, results are not real tokenizer-driven tr... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & If synthetic model fallback is used, results are not real Qwen/TinyLlama traces. & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & Prompt set is small and not representative of all user data. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & No black-box query attack is implemented. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & No side-channel attack is implemented. & hardware\_sidechannel\_security & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & No real TEE isolation is evaluated. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & Dense sandwiching reduces tested recovery but does not imply semantic security. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & This stage only evaluates greedy generation traces, not sampling / beam searc... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & Inter-block hidden states (boundary\_input, final) are recovered to plain spac... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
real\_token\_activation\_attacks & fresh\_perm\_only and fresh\_perm\_plus\_sandwich\_plus\_pad share the same per-call... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/real\_token\_activation\_attacks.json \\
stronger\_attackers & These are stronger proxy attacks, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/stronger\_attackers.json \\
stronger\_attackers & Black-box attacks only use generated outputs and logits summaries. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/stronger\_attackers.json \\
stronger\_attackers & Timing results are model-based proxies, not real TEE timing measurements. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/stronger\_attackers.json \\
stronger\_attackers & No hardware side-channel attack is implemented. & hardware\_sidechannel\_security & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/stronger\_attackers.json \\
stronger\_attackers & No real TEE isolation is evaluated. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/stronger\_attackers.json \\
stronger\_attackers & Synthetic fallback results are not real Qwen/TinyLlama results. & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/stronger\_attackers.json \\
stronger\_attackers & Inter-block masking is experimental unless explicitly marked implemented. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/stronger\_attackers.json \\
stronger\_attackers & Dense sandwiching and fresh permutation reduce tested recovery but do not imp... & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/stronger\_attackers.json \\
lora\_training\_experiments & Stage 7.0 is a prototype LoRA private training path, not full Qwen/TinyLlama ... & full\_model\_lora\_finetune & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_training\_experiments & Backward / optimizer update remains trusted in Stage 7.0 — only the forward i... & optimizer\_trusted\_outsourced & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_training\_experiments & Optimizer state (SGD momentum / AdamW moments) is trusted-only and never expo... & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_training\_experiments & PEFT / DeepSpeed / vLLM / FlashAttention are NOT integrated. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_training\_experiments & Real TEE isolation is NOT evaluated; security\_profile stays 'proxy-evaluated,... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_training\_experiments & LoRA adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_training\_experiments & Distributed training is NOT implemented. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_training\_experiments & Reported metrics are summary statistics + fingerprints. Private data, raw ada... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_training\_experiments & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_experiments.json \\
lora\_security\_proxy & These are proxy attacks, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_security\_proxy.json \\
lora\_security\_proxy & LoRA rank ``r`` remains visible from the shape of A\_tilde / B\_tilde unless ex... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_security\_proxy.json \\
lora\_security\_proxy & Optimizer state (SGD momentum / AdamW moments) remains trusted-only in Stage ... & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_security\_proxy.json \\
lora\_security\_proxy & No real TEE isolation is evaluated; security\_profile stays 'proxy-evaluated, ... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_security\_proxy.json \\
lora\_security\_proxy & No full private fine-tuning workload is evaluated; this is a single-linear, t... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_security\_proxy.json \\
lora\_security\_proxy & Adversary model is a passive GPU observer of (X\_tilde, W\_tilde, A\_tilde, B\_ti... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_security\_proxy.json \\
lora\_security\_proxy & Hardware side-channel attacks (cache / power / EM) are NOT evaluated. & hardware\_sidechannel\_security & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_security\_proxy.json \\
lora\_security\_proxy & Adapter is NEVER merged into the public base weight W (constraint 7). & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_security\_proxy.json \\
lora\_backward\_experiments & Stage 7.1 implements masked-gradient prototype, not full private fine-tuning. & other & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & Loss computation remains trusted (G = dL/dY is computed on the trusted side). & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & Optimizer update remains trusted (SGD momentum / AdamW m, v never cross the b... & optimizer\_trusted\_outsourced & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & PEFT / DeepSpeed / vLLM / FlashAttention are NOT integrated. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & This is not real TEE training; security\_profile stays 'proxy-evaluated, not f... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & Rank padding is NOT implemented; LoRA rank r is still visible from A\_tilde / ... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & LoRA adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & Distributed training is NOT implemented. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & Reports publish summary metrics + fingerprints only. Private data, raw adapte... & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_backward\_experiments & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_backward\_experiments.json \\
lora\_gradient\_security\_proxy & These are gradient-side proxy attacks, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_gradient\_security\_proxy.json \\
lora\_gradient\_security\_proxy & Gradient tensors may leak rank (`grad\_A\_tilde` is (d\_in × r); `grad\_B\_tilde` ... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_gradient\_security\_proxy.json \\
lora\_gradient\_security\_proxy & Optimizer state remains trusted-only in Stage 7.1; SGD momentum / AdamW (m, v... & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_gradient\_security\_proxy.json \\
lora\_gradient\_security\_proxy & Loss / upstream gradient computation remains trusted-only; only G\_tilde cross... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_gradient\_security\_proxy.json \\
lora\_gradient\_security\_proxy & No real TEE isolation is evaluated; security\_profile stays 'proxy-evaluated, ... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_gradient\_security\_proxy.json \\
lora\_gradient\_security\_proxy & No full model LoRA fine-tuning is evaluated; this is a single-linear, tiny-di... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_gradient\_security\_proxy.json \\
lora\_gradient\_security\_proxy & Adversary model is a passive GPU observer of (X\_tilde, W\_tilde, A\_tilde, B\_ti... & hardware\_sidechannel\_security & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_gradient\_security\_proxy.json \\
lora\_gradient\_security\_proxy & Adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_gradient\_security\_proxy.json \\
lora\_rank\_padding\_experiments & Stage 7.2 hides true rank from tensor shape by exposing padded rank. & padded\_rank\_hidden & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & Padded rank r\_pad remains visible to the GPU. & padded\_rank\_hidden & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & Dummy rank indistinguishability is evaluated by proxy (Stage 7.2 security pro... & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & Optimizer state remains trusted-only and is sized to true\_rank, never padded\_... & padded\_rank\_hidden & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & Backward / loss computation remains trusted (Stage 7.1 contract): only G\_tild... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & No PEFT / DeepSpeed / vLLM / FlashAttention integration. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & No real TEE training. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & No full Qwen / TinyLlama / LLaMA LoRA fine-tuning. & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & Adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_padding\_experiments & Reports publish summary metrics + fingerprints. Private data, raw adapters (r... & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_padding\_experiments.json \\
lora\_rank\_security\_proxy & These are rank-leakage proxy attacks, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
lora\_rank\_security\_proxy & True rank is hidden only from shape-level leakage when rank padding is enable... & padded\_rank\_hidden & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
lora\_rank\_security\_proxy & Spectral / statistical rank inference may still be possible depending on dumm... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
lora\_rank\_security\_proxy & paired\_cancellation\_dummy reduces obvious zero-norm leakage but the spectral ... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
lora\_rank\_security\_proxy & No real TEE isolation is evaluated; security\_profile stays 'proxy-evaluated, ... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
lora\_rank\_security\_proxy & Optimizer state remains trusted-only and is sized to true\_rank. & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
lora\_rank\_security\_proxy & Hardware side-channels (cache / power / EM) are NOT evaluated. & hardware\_sidechannel\_security & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
lora\_rank\_security\_proxy & No full model LoRA fine-tuning is evaluated; this is a single-linear, tiny-di... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
lora\_rank\_security\_proxy & Adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_rank\_security\_proxy.json \\
multilayer\_lora\_training\_experiments & This is a synthetic multi-layer LoRA training prototype over a tiny tile, not... & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & Loss + optimizer remain trusted-side (Stage 7.1 contract). & optimizer\_trusted\_outsourced & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & Optimizer state is sized to true\_rank, never padded\_rank, for every LoRA module. & padded\_rank\_hidden & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & Padded rank r\_pad remains visible from tensor shape; only true\_rank is hidden... & padded\_rank\_hidden & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & No PEFT / DeepSpeed / vLLM / FlashAttention integration. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & No real TEE training; security\_profile remains 'proxy-evaluated, not formal'. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & No distributed training. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & Attention is a simple scaled-dot-product proxy, not a correctness benchmark f... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & Adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_training\_experiments & Reports publish summary metrics + fingerprints; private data, raw adapters, r... & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_training\_experiments.json \\
multilayer\_lora\_security\_proxy & These are cross-layer leakage proxy attacks, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & True rank is hidden from shape-level leakage only when padded\_rank is shared ... & padded\_rank\_hidden & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & padded\_rank itself remains visible from tensor shape. & padded\_rank\_hidden & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & Spectral / gradient rank inference may still narrow the attacker's range, esp... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & Cross-layer linkability under fixed\_masks\_shared\_u is reported high by constr... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & No real TEE isolation is evaluated; security\_profile stays 'proxy-evaluated, ... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & Optimizer state remains trusted-only and is sized to true\_rank for every module. & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & Hardware side-channels (cache / power / EM) are NOT evaluated. & hardware\_sidechannel\_security & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & No full Qwen / TinyLlama / LLaMA fine-tuning is evaluated; this is a multi-li... & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
multilayer\_lora\_security\_proxy & Adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/multilayer\_lora\_security\_proxy.json \\
lora\_training\_timing\_proxy & Timing results are proxy estimates from an FLOP-and-boundary cost model, not ... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_training\_timing\_proxy & Constant-time training mode is simulated (upper-bound latency padding); the p... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_training\_timing\_proxy & Hardware side-channels (cache / power / EM) are NOT evaluated. & hardware\_sidechannel\_security & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_training\_timing\_proxy & Cost-model constants are coarse; absolute latencies are illustrative. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_training\_timing\_proxy & Leakage classifier uses bucket-mean labels as the attacker; this is a generou... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_training\_timing\_proxy & No PEFT / DeepSpeed / vLLM / FlashAttention integration. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_training\_timing\_proxy & No real Qwen / TinyLlama / LLaMA fine-tuning; this is a multi-linear cost-mod... & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_training\_timing\_proxy & Adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_training\_timing\_proxy & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_training\_timing\_proxy.json \\
lora\_stronger\_dummy\_experiments & Stronger dummy distributions are proxy-evaluated, not formal. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_experiments & Padded rank remains visible unless heterogeneous padded\_rank is separately en... & padded\_rank\_hidden & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_experiments & Spectral hardening does not imply cryptographic hiding. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_experiments & Optimizer state remains trusted-only and is sized to true\_rank for every LoRA... & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_experiments & No real TEE training is evaluated; security\_profile stays 'proxy-evaluated, n... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_experiments & No full Qwen / TinyLlama / LLaMA LoRA fine-tuning is evaluated; this is a sin... & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_experiments & No PEFT / DeepSpeed / vLLM / FlashAttention integration. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_experiments & Adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_experiments & Reports publish summary metrics + fingerprints only; private data, raw adapte... & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_experiments.json \\
lora\_stronger\_dummy\_security\_proxy & These are stronger-dummy proxy attacks, not formal security proofs. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
lora\_stronger\_dummy\_security\_proxy & Padded rank r\_pad remains visible from tensor shape — only true\_rank is hidden. & padded\_rank\_hidden & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
lora\_stronger\_dummy\_security\_proxy & Spectral hardening does not imply cryptographic hiding. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
lora\_stronger\_dummy\_security\_proxy & Dummy strategy classification uses a generous bucket-mean attacker model. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
lora\_stronger\_dummy\_security\_proxy & No real TEE isolation is evaluated; security\_profile stays 'proxy-evaluated, ... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
lora\_stronger\_dummy\_security\_proxy & Optimizer state remains trusted-only and is sized to true\_rank. & optimizer\_trusted\_outsourced & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
lora\_stronger\_dummy\_security\_proxy & Hardware side-channels (cache / power / EM) are NOT evaluated. & hardware\_sidechannel\_security & medium & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
lora\_stronger\_dummy\_security\_proxy & No full Qwen / TinyLlama / LLaMA LoRA fine-tuning is evaluated; this is a sin... & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
lora\_stronger\_dummy\_security\_proxy & Adapter is NEVER merged into the public base weight W. & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/lora\_stronger\_dummy\_security\_proxy.json \\
paper\_toy\_tasks & Task-like inputs are deterministic synthetic sequences; no external dataset, ... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_toy\_tasks & Targets are deterministic functions of input ids and do NOT reflect any real-... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_toy\_tasks & The toy model is a tiny LoRA-augmented decoder stack; not a full Qwen / TinyL... & full\_model\_lora\_finetune & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_toy\_tasks & Loss, optimizer, and backward remain trusted-side (Stage 7.0 / 7.1 contract). & optimizer\_trusted\_outsourced & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_toy\_tasks & Local CPU runtime only; not real TEE wall-time and not GPU throughput. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_toy\_tasks & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_toy\_tasks & No PEFT / DeepSpeed / vLLM / FlashAttention integration. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_toy\_tasks & Adapter is NEVER merged into the public base weight W (Stage 7.0 contract). & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_toy\_tasks & Reports publish summary metrics only; raw tensors / masks / adapters / gradie... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_toy\_tasks.json \\
paper\_baseline\_comparison & All risk levels are proxy-derived from the mitigation configuration and the e... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_baseline\_comparison.json \\
paper\_baseline\_comparison & Boundary call counts and online extra matmul counts are derived structurally ... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_baseline\_comparison.json \\
paper\_baseline\_comparison & Local CPU runtime only; not real TEE wall-time and not GPU throughput. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_baseline\_comparison.json \\
paper\_baseline\_comparison & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_baseline\_comparison.json \\
paper\_baseline\_comparison & No PEFT / DeepSpeed / vLLM / FlashAttention integration. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_baseline\_comparison.json \\
paper\_baseline\_comparison & Adapter is NEVER merged into the public base weight W (Stage 7.0 contract). & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_baseline\_comparison.json \\
paper\_baseline\_comparison & Reports publish summary metrics only; raw tensors / masks / adapters / gradie... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_baseline\_comparison.json \\
paper\_ablation\_study & All ablation rows reuse the algebraic identities of Theorem 1-9; correctness ... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_ablation\_study.json \\
paper\_ablation\_study & Risk levels are proxy-derived from the existing Stage 5-7 security\_proxy\_summ... & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_ablation\_study.json \\
paper\_ablation\_study & Runtime overhead is local CPU emulation only; not real TEE wall-time and not ... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_ablation\_study.json \\
paper\_ablation\_study & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_ablation\_study.json \\
paper\_ablation\_study & Adapter is NEVER merged into the public base weight W (Stage 7.0 contract). & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_ablation\_study.json \\
paper\_ablation\_study & Reports publish summary metrics only; raw tensors / masks / adapters are neve... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_ablation\_study.json \\
paper\_stability\_study & All identities are exercised on synthetic tiles; no production fine-tune or e... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_stability\_study.json \\
paper\_stability\_study & Sweeps are restricted to CPU local emulation; no GPU, no real TEE, no network... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_stability\_study.json \\
paper\_stability\_study & Stability is measured by the spread of float64 round-off across seeds/shapes;... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_stability\_study.json \\
paper\_stability\_study & Adapter is NEVER merged into the public base weight W (Stage 7.0 contract). & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_stability\_study.json \\
paper\_stability\_study & Reports publish summary statistics only; raw tensors / masks / adapters / gra... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_stability\_study.json \\
paper\_stability\_study & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/paper\_stability\_study.json \\
cpu\_runtime\_completion & This is CPU / local trusted-runtime emulation, NOT real TEE wall-time and NOT... & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/cpu\_runtime\_completion.json \\
cpu\_runtime\_completion & No ``time.sleep`` is used; ``time.perf\_counter`` only. & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/cpu\_runtime\_completion.json \\
cpu\_runtime\_completion & Modern-decoder rows use synthetic stand-ins; no Hugging Face model load and n... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/cpu\_runtime\_completion.json \\
cpu\_runtime\_completion & No PEFT / DeepSpeed / vLLM / FlashAttention integration. & peft\_integration & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/cpu\_runtime\_completion.json \\
cpu\_runtime\_completion & Adapter is NEVER merged into the public base weight W (Stage 7.0 contract). & no\_adapter\_merge & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/cpu\_runtime\_completion.json \\
cpu\_runtime\_completion & Workload sizes are small for pytest stability -- absolute numbers are illustr... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/cpu\_runtime\_completion.json \\
cpu\_runtime\_completion & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/cpu\_runtime\_completion.json \\
cpu\_runtime\_completion & Reports publish summary statistics only; raw tensors / masks / adapters / gra... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/cpu\_runtime\_completion.json \\
direct\_prior\_work\_comparison & Each baseline implements only the primitive(s) listed in its BaselineSelfDecl... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
direct\_prior\_work\_comparison & Cost-model rows (Gazelle / Delphi / SecureML / MiniONN) do not produce measur... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
direct\_prior\_work\_comparison & Threat models differ across baselines; the comparison is *primitive-functiona... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
direct\_prior\_work\_comparison & Amulet decoder rows return UnsupportedResult with mathematical\_reason; Amulet... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
direct\_prior\_work\_comparison & Arrow row is recorded as missing-formula rather than substituted with a gener... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
direct\_prior\_work\_comparison & CryptoNets row is an arithmetic skeleton only; no HE library is used; no encr... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
direct\_prior\_work\_comparison & Local CPU emulation only -- not real TEE wall-time and not GPU throughput. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
direct\_prior\_work\_comparison & No formal / cryptographic / semantic security is claimed for any row. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
direct\_prior\_work\_comparison & Reports publish summary statistics; raw tensors / masks / adapters / gradient... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/direct\_prior\_work\_comparison.json \\
ours\_runtime\_api\_validation & Local CPU backend only; real TEE / GPU backend is NOT implemented. & real\_tee\_wall\_time & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/ours\_runtime\_api\_validation.json \\
ours\_runtime\_api\_validation & ``tee\_gpu\_ready\_interface=True`` means the protocol logic is backend-agnostic... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/ours\_runtime\_api\_validation.json \\
ours\_runtime\_api\_validation & Components are exercised on small synthetic tiles; this is a runtime-API conf... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/ours\_runtime\_api\_validation.json \\
ours\_runtime\_api\_validation & No formal / cryptographic / semantic security is claimed. & formal\_security & high & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/ours\_runtime\_api\_validation.json \\
ours\_runtime\_api\_validation & Reports publish transcript summaries only; raw tensors / masks / adapters / g... & other & low & Treat as a limitation in the paper's threat-model section; do not advertise t... & outputs/ours\_runtime\_api\_validation.json \\
\bottomrule
\end{tabular}
\end{table}
