# Command Records

Each accepted result must have a shell command using archive-relative paths,
pinned arguments, and no credentials, hostnames, usernames, cache locations, or
institutional identifiers. Commands marked missing in the manifest must be
reconstructed and rerun before the corresponding result can be claimed as
reproducible.
