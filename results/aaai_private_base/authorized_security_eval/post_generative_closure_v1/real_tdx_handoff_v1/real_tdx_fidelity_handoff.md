# Later real-TDX fidelity-validation handoff

**Status: PREPARED_NOT_EXECUTED. No TDX resource was requested or configured.**

Frozen samples: 500; metadata-free features: 706; dtype: `fp32`. The JSON freezes IDs, hashes, schema, expected shapes/dtypes, forbidden fields, and the paired comparison procedure. A future authorized owner must preregister precision tolerances before collecting a REAL_TDX_BACKED_VIEW.
