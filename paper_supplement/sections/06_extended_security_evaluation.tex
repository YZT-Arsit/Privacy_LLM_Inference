% ===========================================================================
% APPENDIX C: ATTACK DETAILS --- skeleton (skill S7 hyperparameters). One block
% per evaluated attack with the full configuration table. Clearly separate:
% executed attacks, setup-blocked attacks (NOT defeated), and strawman/null
% attacks (NOT security evidence).
% ===========================================================================
\section{Attack Details}
\label{app:attacks}

% For each attack: attacker knowledge; input; target secret; objective; #samples;
% init; stopping; metric; success threshold; seed policy; box-level; environment.
%
% CANONICAL private-base attacker (report transcript-executable configs):
%  - S1-S6 (real Qwen-0.5B secret oracle, CPU): ridge/MLP/SVD/DLG-LBFGS/Shokri-MIA/KV.
%  - Unified matrix, DEFAULT rows (toy/CPU): KPA lstsq (fresh-pad), frequency, EIA/BRE-fwd.
%  - Timing side-channel proxy (op-count).
%
% Setup-blocked (MUST NOT be reported as defeated):
%  - bre_bisr_backward (needs split-FT gradients); nonlinear masked backward
%    (not_implemented); real-activation synthetic fallback.
%
% Strawman/null (MUST NOT be reported as security):
%  - private-weights adapter extraction sets dW_est=zeros then reports success=False;
%    naive "assume-no-mask" extraction; A3 naive Gram-diagonal (stronger graph
%    matching untried).
\TODO{Populate from results/attacks/, outputs/attacks/, results/aaai\_private\_base/security/; cite attack\_result\_schema.md.}
